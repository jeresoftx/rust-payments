# Ejercicio 09: una política explica su decisión

Prueba cada combinación relevante de `unknown_device` y `high_frequency`.
Explica por qué el modelo devuelve `Challenge` antes de `Reject` cuando solo
una señal de riesgo está presente. La [solución](../../examples/09_risk.rs) no
estima fraude ni identifica a una persona.
