# Ejercicio 06: cambia la implementación, conserva el puerto

Usa la misma `ProviderRequest` con `AcceptingProvider` y `RejectingProvider`.
¿Qué parte del código del llamador cambia? La
[solución](../../examples/06_provider.rs) muestra que el contrato interno se
conserva mientras el adaptador cambia su respuesta sintética.
